Template com Nuxt3, TailwindCSS e Electron.
Packing using Electron Builder.

```bash
# Install dependencies
npm install

# build and generate the app
#For windows users: run as administrator
npm run build
```
